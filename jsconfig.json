
{
  "compilerOptions": {
    "baseUrl": ".", // Ensures paths are resolved relative to the project root
    "paths": {
      "@lib/*": ["./src/app/*"] // Maps @lib to the src/app directory
    }
  }
}